$(document).ready(function(){
    let footnotes = $('span[data-footnote]');
    if(footnotes){
        footnotes.each(function(index, element){
            let current = $(element);
            
        });
    }
});